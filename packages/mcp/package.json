{
  "name": "@localmemos/mcp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node ../node/scripts/build-native.mjs && tsc -p tsconfig.json",
    "test": "node ../node/scripts/build-native.mjs && vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
