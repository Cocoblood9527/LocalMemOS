[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "memory-sdk"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.maturin]
module-name = "memory_sdk._memory_sdk"
python-source = "."
